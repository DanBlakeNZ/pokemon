import pokeBallIcon from '../../assets/poke_ball_icon.svg'

function Spinner() {
  return <img src={pokeBallIcon} alt="Poke Ball Spinner" className="animate-spin h-full w-full" />
}

export default Spinner
